[pytest]
testpaths = tests
asyncio_default_fixture_loop_scope = function
markers =
    unit: Focused source-contract checks for data and metadata.
    smoke: High-signal checks that the public site sources are coherent and safe to publish.
